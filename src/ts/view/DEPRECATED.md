This folder contains the legacy `PageView` implementation that has been replaced by React-based UI under `src/ts/ui`.

Files here are kept for reference. You can safely remove this folder once you've confirmed the React UI covers all behavior.
